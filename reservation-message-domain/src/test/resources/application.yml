spring:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  datasource:
    url: jdbc:h2:mem:testdb;
    driver-class-name: org.h2.Driver
    username: sa
    password:

aws:
  s3:
    region: REGION
    bucket-name: BUCKET
    directory: DIRECTORY
    access-key: ACCESS_KEY
    secret-key: SECRET_KEY
